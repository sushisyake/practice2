$("#com-field").html("<%= j(render 'book_comments/book-com', book: @book) %>");
$("textarea").val('');

<!--id="comments_areaにをrenderで指定した部分テンプレートに更新-->
<!--$("#comments_area").html("<#%= j(render 'comments/post_comments', post: @post, comment: @comment) %>");-->
<!--フォームを送信したあとにフォームの値を空にします-->
<!--$("textarea").val('');-->